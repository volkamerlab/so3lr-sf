coverage:
  status:
    project:
      default:
        target: 85%
        threshold: 5%
    patch:
      default:
        target: 85%
        threshold: 5%

  ignore:
    - "tests/*"
    - "setup.py"

comment:
  layout: "reach, diff, flags, files"
  behavior: default
  require_changes: false